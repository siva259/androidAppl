// Top-level build file where you can add configuration options common to all sub-projects/modules.
apply plugin: 'maven'

buildscript {
    
    repositories {
        google()
        jcenter()
        maven {
          url "http://35.226.54.253:8081/repository/maven-releases/"
    }
        
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.2'
        

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}



allprojects {
    repositories {
        google()
        jcenter()
        
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: "http://35.226.54.253:8081/repository/maven-releases/") {
                authentication(userName: "admin", password: "Test@123")
                pom.groupId = "com.examples.android"
                pom.artifactId = "pocAndroid"
                pom.version = '1.0'
            }
        }
    }
}
